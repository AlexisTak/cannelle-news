import{describe,expect,it,vi}from"vitest";import{callMediaProcessor}from"./processor";import type{MediaAsset,MediaJob}from"./domain";const asset={id:"a",mediaId:"a",filename:"a.jpg",mimeType:"image/jpeg",size:10,sourceUrl:"https://site/a.jpg",title:"A",alt:"",caption:"",tags:[],ocrText:"",status:"ready",createdAt:"",updatedAt:""}satisfies MediaAsset;const job={id:"j",mediaId:"a",type:"ocr",status:"pending",createdAt:""}satisfies MediaJob;describe("media processor",()=>{it("authentifie et normalise la réponse",async()=>{const fetch=vi.fn().mockResolvedValue(new Response(JSON.stringify({ocrText:"texte",size:5}),{status:200}));const ctx={kv:{get:vi.fn(async(k:string)=>k.includes("Endpoint")?"https://processor.test/run":"secret")},http:{fetch}} as never;await expect(callMediaProcessor(job,asset,ctx)).resolves.toMatchObject({ocrText:"texte",size:5});expect(fetch).toHaveBeenCalledWith("https://processor.test/run",expect.objectContaining({method:"POST"}))});});
