export type PaywallMode="soft"|"hard"|"metered";export interface Plan{id:string;name:string;priceCents:number;currency:string;interval:"month"|"year";active:boolean;createdAt:string}export interface Subscription{id:string;customerId:string;planId:string;status:"trialing"|"active"|"past_due"|"cancelled";currentPeriodEnd:string;providerRef?:string;createdAt:string}export interface Coupon{id:string;code:string;percentOff:number;active:boolean;expiresAt?:string;maxUses?:number;uses:number;createdAt:string}export function hasEntitlement(items:Subscription[],now=new Date()):boolean{return items.some(x=>(x.status==="active"||x.status==="trialing")&&new Date(x.currentPeriodEnd)>now)}export function shouldBlock(mode:PaywallMode,views:number,freeViews:number,entitled:boolean):boolean{if(entitled)return false;if(mode==="soft")return false;if(mode==="hard")return true;return views>=freeViews}export function applyCoupon(cents:number,coupon:Coupon|null,now=new Date()):number{if(!coupon||!coupon.active||(coupon.expiresAt&&new Date(coupon.expiresAt)<=now)||(coupon.maxUses!==undefined&&coupon.uses>=coupon.maxUses))return cents;return Math.max(0,Math.round(cents*(100-Math.max(0,Math.min(100,coupon.percentOff)))/100));}
