import{describe,expect,it,vi}from"vitest";import{analyzeClaim}from"./analyzer";import type{Claim}from"./domain";const claim:Claim={id:"c",text:"La Terre est ronde",status:"draft",verdict:"unverified",confidence:0,rationale:"",createdAt:"",updatedAt:""};describe("fact analyzer",()=>{it("borne la confiance et conserve la revue humaine",async()=>{const ctx={kv:{get:vi.fn(async(k:string)=>k.endsWith("aiEnabled")?true:k.endsWith("aiEndpoint")?"https://ai.test/check":"secret")},http:{fetch:vi.fn().mockResolvedValue(new Response(JSON.stringify({verdict:"true",confidence:120,rationale:"Sources concordantes"}),{status:200}))}}as never;await expect(analyzeClaim(claim,ctx)).resolves.toEqual({verdict:"true",confidence:100,rationale:"Sources concordantes"})});});
